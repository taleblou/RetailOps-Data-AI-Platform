# Worker tests

Covers the modular worker engine, durable queue handling, and execution of built-in batch jobs.

## Directory contents

- `test_worker_service.py` — validates queue persistence, batch execution, and CLI-driven worker behavior.
