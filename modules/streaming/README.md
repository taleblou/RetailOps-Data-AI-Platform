# Streaming Module

Builds deployment-ready streaming bundles around Redpanda topics, consumer groups, and processors.

Use the module API route or `scripts/generate_pro_platform_bundle.py` to materialize artifacts under `data/artifacts/pro_platform/streaming/`.
