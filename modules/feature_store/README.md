# Feature Store Module

Builds deployment-ready Feast bundles around feature views, materialization schedules, and compose overlays.

Use the module API route or `scripts/generate_pro_platform_bundle.py` to materialize artifacts under `data/artifacts/pro_platform/feature_store/`.
