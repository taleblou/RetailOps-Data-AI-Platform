# Query Layer Module

Builds deployment-ready Trino bundles around catalog properties, federation SQL, and compose overlays.

Use the module API route or `scripts/generate_pro_platform_bundle.py` to materialize artifacts under `data/artifacts/pro_platform/query_layer/`.
