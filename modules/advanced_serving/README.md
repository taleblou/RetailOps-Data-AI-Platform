# Advanced Serving Module

Builds deployment-ready BentoML bundles around runtime deployments, routing, and compose overlays.

Use the module API route or `scripts/generate_pro_platform_bundle.py` to materialize artifacts under `data/artifacts/pro_platform/advanced_serving/`.
