# Metadata Module

Builds deployment-ready OpenMetadata bundles around service connections, lineage ingestion, and compose overlays.

Use the module API route or `scripts/generate_pro_platform_bundle.py` to materialize artifacts under `data/artifacts/pro_platform/metadata/`.
