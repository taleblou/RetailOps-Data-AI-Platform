# Cdc Module

Builds deployment-ready CDC bundles around Debezium, topic routing, and compose overlays.

Use the module API route or `scripts/generate_pro_platform_bundle.py` to materialize artifacts under `data/artifacts/pro_platform/cdc/`.
