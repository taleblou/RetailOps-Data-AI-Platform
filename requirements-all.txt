# Full Python dependency set for the complete RetailOps repository.
# Use this file when you want one installation manifest that covers:
# - core runtime services
# - all optional platform extensions
# - reporting modules
# - tests and developer tooling
#
# Example:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements-all.txt

fastapi>=0.115,<1.0
httpx>=0.28.1
psycopg[binary]>=3.2,<4.0
pydantic>=2.10,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.12
PyYAML>=6.0,<7.0
tenacity>=8.2,<9.0
uvicorn[standard]>=0.30,<1.0

# Reporting modules
pypdf>=5,<6
reportlab>=4,<5

# Optional platform extensions
feast>=0.40,<1.0
bentoml>=1.3,<2.0

# Test and developer tooling used by the repository
pytest>=8.3,<9
dbt-core>=1.11.7
dbt-postgres>=1.10.0
mypy>=1.11,<2
pre-commit>=4,<5
ruff>=0.6,<1
